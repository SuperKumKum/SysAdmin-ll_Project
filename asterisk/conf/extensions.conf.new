; fichier de configuration des extensions

[direction]
exten => 200,1,Dial(SIP/300, 15)		; appelle le numéro de la secrétaire pendant 15s 
exten => 300,2,VoiceMail(${exten}@secretaire)	; apres 15s, renvoie vers la boite vocale
exten => _1XXX,1,Dial(SIP/${exten}, 15)		; appelle dans la range des ouvriers 
exten => _1XXX,2,VoiceMail(${exten@ouvrier)	; 

[market]
exten => 1301, 1, Dial(SIP/1302, 15)
